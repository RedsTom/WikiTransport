# Codebase refactoring — no visible changes

Summary: Internal code reorganization for better maintainability — no visible changes for end users

Date: 2026-05-16

- Extracted viewport state and pan/zoom logic from PlanView into a reusable `useViewport.svelte.ts` module
- Created `StationLabelControls` and `ViewManager` components to reduce large file sizes
- Split `ProjectExportService` into dedicated export and import services
- Applied DRY and SRP principles across the editor codebase
